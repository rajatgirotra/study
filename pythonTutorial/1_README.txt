The way to organize this pythonTutorial folder is to create  individual project folders using "uv init <ProjectFolder> --app/--lib/--package/--script".
then have a pyproject.toml file for each project. each project will have its own uv created virtual environment.
