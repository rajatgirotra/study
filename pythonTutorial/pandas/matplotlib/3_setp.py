# plt.setp() and plt.getp() functions are available to set and get properties of a line or a list of lines
# plt.setp(lines) # will display a long list of all properties which can be set.
# plt.setp(lines, 'linestyle') will display all possible values of linestyles
# plt.setp(lines, linestyle=2, color='r') will set linestyle and color on all lines.

# plt.getp(line, 'linestyle') will give you the linestyle value set
# plt.getp(line) will give you all the line properties and its values
