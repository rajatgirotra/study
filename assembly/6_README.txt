Inout Operand Format
===================

The format is similar to Output operands, however the constraint cannot begin with a '+' or '='
Mostly you'll only use

"rm" (myVar)

For more complex use cases we'll return here when needed.

