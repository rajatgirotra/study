Difference between Dockerfile and docker-compose.yml

A docker file is used to build a docker image. and a docker-compose is used to specify how to run those services
So they both complement each other.

Let's quickly build an image using Dockerfile and then deploy it using docker-compose command.

See composetest folder
