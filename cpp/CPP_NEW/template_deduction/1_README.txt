template arguments types for function templates can already be deduced by the compiler based on function argument types.
beginning with C++17, template arguments for class templates can also be deduced automatically. 
